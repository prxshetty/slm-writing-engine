import{_ as t,o as n,c as o,a0 as r}from"./chunks/framework.BWpMpcZ2.js";const p=JSON.parse('{"title":"AI Assist","description":"","frontmatter":{},"headers":[],"relativePath":"ai-assist.md","filePath":"ai-assist.md"}'),a={name:"ai-assist.md"};function s(i,e,l,h,d,c){return n(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="ai-assist" tabindex="-1">AI Assist <a class="header-anchor" href="#ai-assist" aria-label="Permalink to &quot;AI Assist&quot;">​</a></h1><p>The AI Assist panel is your main interface for writing with AI. You can toggle between <strong>Edit</strong> and <strong>Chat</strong> modes at any time during a session.</p><h2 id="agent-harnesses" tabindex="-1">Agent Harnesses <a class="header-anchor" href="#agent-harnesses" aria-label="Permalink to &quot;Agent Harnesses&quot;">​</a></h2><p>If you have a subscription to an agentic coding tool, you can run it directly from Margin instead of using an API endpoint. Supported harnesses: <strong>OpenCode</strong>, <strong>Claude Code</strong>, <strong>Codex</strong>, and <strong>Antigravity</strong>.</p><ul><li><strong>Setup</strong>: install and authenticate each CLI in your own terminal first — Margin never handles harness credentials. Pick a default in <strong>Settings &gt; Harnesses</strong>, or override per request with the harness dropdown in the Assist panel (the inline bubble always follows the default).</li><li><strong>How it works</strong>: your instruction (plus any selected text) is sent to the agent, which runs in your workspace and edits files itself. Its output streams live in the panel as progress — it is never inserted into the document directly. Harness runs skip the endpoint Planner: the agent receives your instruction, any selected or cursor-anchored text, past conversation and recent edits (bounded by Session Memory), and pointers to the workspace index manifests, and works out everything else — including which files to change or create — by itself. The harnesses&#39; standing edit instructions (including the rule that changes land in files, not in the reply) are editable in <strong>Settings &gt; Context</strong> under <em>Agent Prompts</em>.</li><li><strong>No locking</strong>: the editor stays editable while the agent works. Your current content is snapshotted when the run starts; when it finishes, agent changes are three-way merged (paragraph-level) with any edits you made meanwhile. Your newer work is never silently overwritten — if you both touched the same paragraph, your version is kept and you&#39;re told about the conflict.</li></ul><blockquote><p>See <a href="./configuration/harnesses.html">Harnesses</a> for detection, custom executable paths, and troubleshooting.</p></blockquote><h2 id="edit-mode" tabindex="-1">Edit Mode <a class="header-anchor" href="#edit-mode" aria-label="Permalink to &quot;Edit Mode&quot;">​</a></h2><p>Use Edit mode when you want the AI to modify or add content. The behavior depends on whether you have text selected or just a cursor placed.</p><h3 id="replace-with-text-selected" tabindex="-1">Replace (with text selected) <a class="header-anchor" href="#replace-with-text-selected" aria-label="Permalink to &quot;Replace (with text selected)&quot;">​</a></h3><ol><li><strong>Highlight</strong> the text you want to change.</li><li>A <strong>Writing Bubble Menu</strong> appears with formatting options, a node selector (paragraph/heading levels), and action buttons.</li><li>Click <strong>Rewrite</strong> to open an inline instruction input (press Escape to cancel).</li><li>Type your instruction (e.g., &quot;Make this more dramatic&quot;, &quot;Shorten to two sentences&quot;) and press Enter.</li><li>The AI rewrites only the selected portion -- surrounding text stays untouched. The inline bubble always follows your default harness from Settings.</li></ol><p><strong>How replacement works:</strong></p><p>The backend finds which paragraph contains your selection and extracts context:</p><ul><li><strong>Full paragraph selection</strong>: If you select an entire paragraph, the backend sends the paragraph above, your target, and the paragraph below as a three-paragraph window.</li><li><strong>Sub-paragraph selection</strong>: If you select a sentence or phrase within a paragraph, the backend extracts the surrounding text <em>within that same paragraph</em> as before/after context. This allows the Writer to match rhythm and tone of the surrounding sentences.</li></ul><p>The Writer agent receives this context plus your instruction and generates new text. The generated text then replaces your selected content directly -- the frontend deletes the selection and inserts the AI output in its place.</p><p>The Writer is instructed to return either a short snippet (if you asked to tweak a specific phrase) or a full rewrite (if you asked to rewrite the entire paragraph). Either way, only your selection is replaced -- nothing else in the document is touched.</p><h3 id="insert-with-cursor-only" tabindex="-1">Insert (with cursor only) <a class="header-anchor" href="#insert-with-cursor-only" aria-label="Permalink to &quot;Insert (with cursor only)&quot;">​</a></h3><ol><li><strong>Place your cursor</strong> where you want new content to appear.</li><li>Open the AI Assist panel and switch to <strong>Edit Document</strong>.</li><li>Type your instruction (e.g., &quot;Write a paragraph about the castle&#39;s architecture&quot;).</li><li>Press Enter.</li><li>The AI generates new content and inserts it at your cursor position.</li></ol><p><strong>How insertion works:</strong></p><p>The backend identifies the paragraph containing your cursor and sends the same three-paragraph window (above, target, below) as context. The AI generates fresh content that gets inserted at the cursor -- nothing is replaced, no text is deleted.</p><h2 id="chat-mode" tabindex="-1">Chat Mode <a class="header-anchor" href="#chat-mode" aria-label="Permalink to &quot;Chat Mode&quot;">​</a></h2><p>Use Chat mode for brainstorming, asking questions, or discussing your content with the AI.</p><ul><li>The AI has access to your full <strong>conversation history</strong> within the session.</li><li>If you have text selected when you send a message, the AI can see what you&#39;ve highlighted. If no text is selected, the AI sees the paragraph your cursor is on.</li><li>Prior <strong>edit history</strong> (RECENT_EDITS) is carried into chat context, so the AI is aware of what&#39;s been previously planned or edited.</li><li>The AI also receives the full <strong>active document</strong> as context.</li><li>Chat mode <strong>does not modify your document</strong> -- it only responds conversationally.</li><li>You can switch between Edit and Chat modes freely without losing session context.</li></ul><h2 id="context-window" tabindex="-1">Context Window <a class="header-anchor" href="#context-window" aria-label="Permalink to &quot;Context Window&quot;">​</a></h2><p>In Edit mode, the AI receives surrounding context to help it match tone and style:</p><ul><li><strong>Full paragraph selection</strong>: A three-paragraph window (paragraph above, target paragraph, paragraph below).</li><li><strong>Sub-paragraph selection</strong>: The text before and after your selection <em>within the same paragraph</em>, plus the full paragraphs above and below if available.</li></ul><p>This keeps token usage low and focused, which is especially important for smaller local models. The Writer agent is instructed to never echo or reproduce the surrounding paragraphs -- only to produce new text for the target.</p><h2 id="how-context-works" tabindex="-1">How Context Works <a class="header-anchor" href="#how-context-works" aria-label="Permalink to &quot;How Context Works&quot;">​</a></h2><p>The AI Assist panel uses different context strategies depending on the mode:</p><h3 id="input-bar-context" tabindex="-1">Input Bar Context <a class="header-anchor" href="#input-bar-context" aria-label="Permalink to &quot;Input Bar Context&quot;">​</a></h3><p>The <code>@filename</code> chips and selection tag in the input area serve as a user reference visible in history logs. File content is resolved independently by each agent.</p><h3 id="edit-mode-planner-writer" tabindex="-1">Edit Mode (Planner + Writer) <a class="header-anchor" href="#edit-mode-planner-writer" aria-label="Permalink to &quot;Edit Mode (Planner + Writer)&quot;">​</a></h3><p>The <strong>Planner</strong> scans available workspace manifests and document structure to determine which context files are relevant. It receives the document outline (optional), selected or cursor-anchored text, and prior edit history.</p><p>The <strong>Writer</strong> is stateless -- each edit request is processed independently. It receives only the three-paragraph window around your cursor plus the files resolved by the Planner.</p><h3 id="bubble-menu-rewrite-skip-planner" tabindex="-1">Bubble Menu Rewrite (skip Planner) <a class="header-anchor" href="#bubble-menu-rewrite-skip-planner" aria-label="Permalink to &quot;Bubble Menu Rewrite (skip Planner)&quot;">​</a></h3><p>The Writing Bubble Menu&#39;s <strong>Rewrite</strong> button provides a faster path for direct rewrites. It bypasses the Planner entirely and sends your instruction directly to the Writer with only the paragraph context. Use this for quick, focused edits when you don&#39;t need workspace context (character files, style guides, etc.).</p><p>Both panel edits and inline Rewrite use the same Writer template (<code>prompts/simple-writer.md</code>) — there is no separate inline prompt to maintain. Edit that file to change rewrite behavior everywhere at once.</p><h3 id="chat-mode-1" tabindex="-1">Chat Mode <a class="header-anchor" href="#chat-mode-1" aria-label="Permalink to &quot;Chat Mode&quot;">​</a></h3><p>The <strong>Chat</strong> agent receives the full active document, any selected text or cursor-anchored paragraph, and prior edit history (RECENT_EDITS) from the session. It maintains its own conversation history using only chat-mode logs.</p><h3 id="user-preferences" tabindex="-1">User Preferences <a class="header-anchor" href="#user-preferences" aria-label="Permalink to &quot;User Preferences&quot;">​</a></h3><p>For persistent style rules (spelling, voice, paragraph length), edit the agent prompts in <strong>Settings &gt; Context</strong> — see <a href="./configuration/prompts.html">Prompts</a>. The Planner intentionally receives instruction-neutral context, keeping its file selection unbiased.</p><h2 id="reasoning-thinking" tabindex="-1">Reasoning &amp; Thinking <a class="header-anchor" href="#reasoning-thinking" aria-label="Permalink to &quot;Reasoning &amp; Thinking&quot;">​</a></h2><p>Some AI models produce internal reasoning (or &quot;thinking&quot;) before generating their final response. When this happens, the reasoning text appears in a collapsible <strong>Thought Process</strong> dropdown above the response.</p><h3 id="during-streaming" tabindex="-1">During Streaming <a class="header-anchor" href="#during-streaming" aria-label="Permalink to &quot;During Streaming&quot;">​</a></h3><p>While the model is generating, the reasoning text streams in live and the dropdown opens automatically. You can close it to focus on the final output as it arrives.</p><h3 id="in-history" tabindex="-1">In History <a class="header-anchor" href="#in-history" aria-label="Permalink to &quot;In History&quot;">​</a></h3><p>Past AI responses that included reasoning show a collapsed <strong>Thought Process</strong> button. Click it to review the model&#39;s reasoning after the fact.</p><h3 id="how-to-control-it" tabindex="-1">How to Control It <a class="header-anchor" href="#how-to-control-it" aria-label="Permalink to &quot;How to Control It&quot;">​</a></h3><p>You can configure the thinking behavior per endpoint in <strong>Settings &gt; Endpoints</strong>:</p><ul><li><strong>Thinking Model toggle</strong>: Turn reasoning on or off for a specific endpoint. When off, the model&#39;s output passes through unmodified.</li><li><strong>Custom Thinking Tags</strong>: Some models use non-standard reasoning tags. You can add custom <code>(open, close)</code> tag pairs so the system can strip them from the output you see.</li><li><strong>Show thinking by default</strong>: Set whether the Thought Process dropdown opens automatically for new responses.</li></ul><blockquote><p>See <a href="./configuration/endpoints.html#reasoning-settings">Endpoints &gt; Reasoning Settings</a> for full configuration details.</p></blockquote><h2 id="debugging-telemetry" tabindex="-1">Debugging &amp; Telemetry <a class="header-anchor" href="#debugging-telemetry" aria-label="Permalink to &quot;Debugging &amp; Telemetry&quot;">​</a></h2><p>Each AI request and response is logged locally. Click the <strong>telemetry / book icon</strong> in the AI Assist panel to open the inspector, which shows:</p><ul><li>The last API request (your full input)</li><li>The complete system prompt sent to the AI</li><li>The AI&#39;s raw output response</li><li>Session context and remaining context window</li><li>Total conversation history for the session</li></ul><p>Use this to understand why the AI responded a certain way, debug quality issues, or inspect what prompts were used.</p><blockquote><p>See <a href="./configuration/debugging.html">Debugging</a> for more details on logs, prompt templates, and local telemetry.</p></blockquote>',55)])])}const g=t(a,[["render",s]]);export{p as __pageData,g as default};
